import 'package:flutter/material.dart';
import 'package:meet_scheduler/Services/signin.dart';

class Authenticate extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: SignIn(),
    );
  }
}
